name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    if: >-
      !startsWith(github.event.head_commit.message, 'docs')
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine version bump
        id: bump
        run: |
          LAST_TAG=$(git tag --sort=-v:refname | head -1)
          if [ -z "$LAST_TAG" ]; then
            RANGE="HEAD"
            LAST_TAG="v0.0.0"
          else
            RANGE="${LAST_TAG}..HEAD"
          fi

          MAJOR=$(echo "$LAST_TAG" | sed 's/^v//' | cut -d. -f1)
          MINOR=$(echo "$LAST_TAG" | sed 's/^v//' | cut -d. -f2)
          PATCH=$(echo "$LAST_TAG" | sed 's/^v//' | cut -d. -f3)

          COMMITS=$(git log "$RANGE" --pretty=format:"%s")
          if [ -z "$COMMITS" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          HAS_BREAKING=$(echo "$COMMITS" | grep -cE "^[a-z]+(\(.+\))?!:|BREAKING CHANGE" || true)
          HAS_FEAT=$(echo "$COMMITS" | grep -cE "^feat" || true)
          HAS_PATCH=$(echo "$COMMITS" | grep -cE "^(fix|refactor|perf|chore|ci|build|test)" || true)

          if [ "$HAS_BREAKING" -gt 0 ]; then
            MAJOR=$((MAJOR + 1)); MINOR=0; PATCH=0
          elif [ "$HAS_FEAT" -gt 0 ]; then
            MINOR=$((MINOR + 1)); PATCH=0
          elif [ "$HAS_PATCH" -gt 0 ]; then
            PATCH=$((PATCH + 1))
          else
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}"
          echo "version=$NEW_VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$NEW_VERSION" >> "$GITHUB_OUTPUT"
          echo "skip=false" >> "$GITHUB_OUTPUT"
          echo "range=$RANGE" >> "$GITHUB_OUTPUT"
          echo "Bumping to $NEW_VERSION"

      - name: Update pyproject.toml version
        if: steps.bump.outputs.skip == 'false'
        run: |
          sed -i 's/^version = ".*"$/version = "${{ steps.bump.outputs.version }}"/' pyproject.toml
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add pyproject.toml
          git commit -m "chore: bump version to ${{ steps.bump.outputs.version }} [skip ci]"
          git push

      - name: Generate changelog
        if: steps.bump.outputs.skip == 'false'
        id: changelog
        run: |
          RANGE="${{ steps.bump.outputs.range }}"
          {
            echo 'body<<CHANGELOG_EOF'

            FEATS=$(git log "$RANGE" --pretty=format:"%s" | grep -E "^feat" | sed 's/^feat[^:]*: //' | sed 's/^/- /' || true)
            if [ -n "$FEATS" ]; then
              echo "## Features"
              echo "$FEATS"
              echo ""
            fi

            FIXES=$(git log "$RANGE" --pretty=format:"%s" | grep -E "^fix" | sed 's/^fix[^:]*: //' | sed 's/^/- /' || true)
            if [ -n "$FIXES" ]; then
              echo "## Bug Fixes"
              echo "$FIXES"
              echo ""
            fi

            REFACTOR=$(git log "$RANGE" --pretty=format:"%s" | grep -E "^refactor" | sed 's/^refactor[^:]*: //' | sed 's/^/- /' || true)
            if [ -n "$REFACTOR" ]; then
              echo "## Refactoring"
              echo "$REFACTOR"
              echo ""
            fi

            PERF=$(git log "$RANGE" --pretty=format:"%s" | grep -E "^perf" | sed 's/^perf[^:]*: //' | sed 's/^/- /' || true)
            if [ -n "$PERF" ]; then
              echo "## Performance"
              echo "$PERF"
              echo ""
            fi

            CHORE=$(git log "$RANGE" --pretty=format:"%s" | grep -E "^(chore|ci|build|test)" | sed 's/^[^:]*: //' | sed 's/^/- /' || true)
            if [ -n "$CHORE" ]; then
              echo "## Miscellaneous"
              echo "$CHORE"
              echo ""
            fi

            echo 'CHANGELOG_EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Create release
        if: steps.bump.outputs.skip == 'false'
        run: |
          gh release create "${{ steps.bump.outputs.tag }}" \
            --title "${{ steps.bump.outputs.tag }}" \
            --notes "${{ steps.changelog.outputs.body }}" \
            --target main
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
